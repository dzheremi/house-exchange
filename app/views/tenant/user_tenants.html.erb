<h1>Existing House Requests</h1>
<p>Below is a list of all of your current house requests.</p>
<table class="table table-striped">
  <tr>
    <th>Start Date</th>
    <th>End Date</th>
    <th>Max. Daily Credits</th>
    <th>Suburb</th>
    <th></th>
  </tr>
  <% @tenants.each do |t| %>
  <tr>
    <td><%= t.start.to_formatted_s(:long) %></td>
    <td><%= t.end.to_formatted_s(:long) %></td>
    <td><%= t.maximum_daily_points %></td>
    <td><%= t.suburb %></td>
    <td style="text-align: right;">
      <%= link_to "Edit", {:action=>"edit", :controller=>"tenant", :id=>t}, class: "btn btn-primary" %>
      <%= link_to "Delete", {:action=>"delete", :controller=>"tenant", :id=>t}, class: "btn btn-danger" %>
    </td>
  </tr>
  <% end %>
  <% if @tenants.empty? %>
  <tr>
    <td colspan="4">You have no requests, click the button below to add one.</td>
  </tr>
  <% end %>
</table><br/>
<%= link_to "Add Request", {:action=>"new", :controller=>"tenant"}, class: "btn btn-primary" %>

