<h1>Existing Home Listings</h1>
<p>Below is a list of all of your current house listings.</p>
<table class="table table-striped">
  <tr>
    <th>Title</th>
    <th>Start Date</th>
    <th>End Date</th>
    <th>Daily Credits</th>
    <th>Min. User Rating</th>
    <th></th>
  </tr>
  <% @listings.each do |l| %>
  <tr>
    <td><%= l.property.title %></td>
    <td><%= l.start.to_formatted_s(:long) %></td>
    <td><%= l.end.to_formatted_s(:long) %></td>
    <td><%= l.daily_points %></td>
    <td><%= l.minimum_rating %></td>
    <td style="text-align: right;">
      <%= link_to "Edit", {:action=>"edit", :controller=>"listing", :id=>l}, class: "btn btn-primary" %>
      <%= link_to "Delete", {:action=>"delete", :controller=>"listing", :id=>l}, class: "btn btn-danger" %>
    </td>
  </tr>
  <% end %>
  <% if @listings.empty? %>
  <tr>
    <td colspan="4">You have no listings, click the button below to add one.</td>
  </tr>
  <% end %>
</table><br/>
<%= link_to "Add Listing", {:action=>"new", :controller=>"listing"}, class: "btn btn-primary" %>
